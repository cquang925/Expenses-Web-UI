export interface ExpenseForm {
    store: string | null,
    address: string | null,
    city: string | null,
    state: string | null,
    date: Date | null,
    amount: number | null,
    category: string | null
}
